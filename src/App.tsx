export default function App() {
  return (
    <main style={{ height: '100vh', display: 'grid', placeItems: 'center' }}>
      <div style={{ textAlign: 'center' }}>
        <h1 style={{ fontSize: 'var(--h1)' }}>Step 1: Scaffold ✅</h1>
        <p style={{ fontSize: 'var(--body)', opacity: 0.7 }}>
          If you can see this, the project is running. Next: the design tokens + canvas layer.
        </p>
      </div>
    </main>
  )
}
